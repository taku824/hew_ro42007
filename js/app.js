const $button = document.getElementsById('button');

const clickHandler = (e) => {
  if(e.target.id === button1 || e.target.id === button2 || e.target.id === button2){
      window.alert('準備中...');
  }

};